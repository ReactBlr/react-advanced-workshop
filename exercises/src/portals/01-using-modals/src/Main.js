import React from 'react';

const Main = (props) => {
    const headerStyle = {
        padding: 10,
        height: 600,
        width: '100%',
        textAlign: 'center',
        marginTop: 200
      };

  return (
    <div style={headerStyle} >
      This is the Awesome Home page
    </div>
  );
}

export default Main;
